		<% where_guessed = Guest.where user_id: @current_user.id %> 

		<% attempts = where_guessed.length %>



<% "#{@scores_array}" %>

<% names = [] %>
<% scores = [] %>

<% @scores_array.each do |scoreset| %>
	<% scoreset.each do |key, value| %>
	<% names << User.find(key).name %>
	<% scores << "#{value}%" %>
	<%end%>

<%end %> 

<% count = 0 %>
<% names.each do |name| %>

	<%= "#{scores[count]} #{name} -- with #{(Guest.where user_id:(User.find_by(name: name).id)).length} attempts made<br>" %>
	<% count += 1 %>
<% end %>


